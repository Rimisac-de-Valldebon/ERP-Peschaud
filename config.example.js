// Example configuration file - rename to config.js and update with real credentials
const credentials = {
    users: [
        {
            username: 'example_user',
            password: 'example_password'
        }
    ]
};

module.exports = credentials; 